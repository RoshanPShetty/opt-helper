/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ["lh3.googleusercontent.com", "images.unsplash.com"], // Add the domain for external images
	},
};

export default nextConfig;
